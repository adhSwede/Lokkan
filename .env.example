# Development stage value handling, Later versions will have real config
# Local SQLite database path
DATABASE_URL=sqlite:kanban.db

# GitHub sync (optional, for remote backup)
# Add your fine-grained personal access token with repo read/write access
# Make sure it ONLY has access to the intended repo.
# GITHUB_TOKEN=<your-fine-grained-token>